# Framework Web assíncrono e servidor
fastapi>=0.111.0
uvicorn>=0.30.1

# Cliente oficial para conexão com o Supabase (PostgreSQL)
supabase>=2.5.1

# Cliente HTTP assíncrono para envio de mensagens à API Oficial da Meta (WhatsApp)
httpx>=0.27.0

# Gerenciamento de variáveis de ambiente do arquivo .env
python-dotenv>=1.0.1

# Validação e serialização de dados (dependência do FastAPI)
pydantic>=2.7.4
